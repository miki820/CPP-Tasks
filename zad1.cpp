#include <iostream>

/*
int main() {
    int x;
    int counter = 1;
    int max = 0;
    int answer = 0;
    int previous = -1;

    while (std::cin >> x && x != 0) {
        if (x == previous) {
            counter++;
        } else {
            if (counter > max) {
                max = counter;
                answer = previous;
            }
            counter = 1;
            previous = x;
        }
    }

    if (counter > max) {
        max = counter;
        answer = previous;
    }

    std::cout << "Longest sequence: " << max << " times " << answer << std::endl;
    return 0;
}
*/

